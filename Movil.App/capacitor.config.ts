import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'js.soluc.tech.start',
  appName: 'Fast-Invent',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
